import { Button, Icon } from "./LikeButton";

const SendButton = () => {
   return (
      <Button>
         <Icon className="far fa-paper-plane outlined"></Icon>
      </Button>
   );
};

export default SendButton;
